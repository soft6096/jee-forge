# <模块名> 模块需求（0.9 需求入口整形产物）

> 本文件为 `/req-intake` 为单个 Java 模块产出的模块级需求文档。
> 用途：作为该模块 1.1 `/feature-list`（功能清单）的**输入**；本模块已有历史产物时，1.1 前必须先走 0.8 `/change-impact`。

## 0. 速览

| 项 | 内容 |
|---|---|
| 模块名 | <模块名> |
| 来源文档 | <原始需求文档名 + 版本/日期> |
| 验收基线 | <以哪份文档哪个版本为准；文档外口头补充需求记录> |
| 依赖模块 | <依赖 / 被依赖> |
| 建议开发顺序 | <第 N 个> |
| 进入 1.1 状态 | ☐ 人确认通过　☐ 待确认 |

## 1. 模块范围

- **做**：<本模块做什么>
- **不做**：<明确排除的内容；属于其他模块/非 Java 服务端/延后>（每个"不做"注明归属或原因，防后续误解）

## 2. 来源段落追踪（防"拆模块时整段漏进/漏出"）

| # | 需求内容 | 来源（原始文档章节） | 备注 |
|---|---|---|---|
| 1 | <一条可独立判定的需求表述> | §3.2 | |
| 2 | … | | |

> 逐节扫过原始文档：本模块覆盖的每一节都应有记录；**无任何段落被整段漏掉**；发现原始文档中有未归入任何模块的段落 → 回到 req-intake 清单确认（补模块 / 明确忽略），不静默消失。

## 3. 需求点初拆提示（给 1.1 打底，不在此展开成功能项）

- <初步识别的需求点方向，供 1.1 feature-list 拆解时承接>

## 4. 跨模块依赖

- <依赖其他模块的接口/数据，及方向；被依赖方先开发>
- <数据归属边界：表/字段属于本模块还是他模块>

## 5. 假设与待澄清

| # | 假设 / 歧义 | 状态 |
|---|---|---|
| 1 | <描述> | 待 1.3 澄清 |

## 6. 备注

- 本模块在 req-intake 批次：<批次时间戳目录路径>
