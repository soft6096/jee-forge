# 需求构成清单与确认（0.9 需求入口整形）

> 本文件为 `/req-intake` 的主产物（批次级）。**非 Java 服务端需求一律不列入本清单。**
> 每份产物在进入 1.1 前须向用户展示并获确认。

## 0. 速览

| 项 | 内容 |
|---|---|
| 原始需求文档 | <文档名 / 版本 / 日期> |
| 触发判据 | ☐ 跨多模块　☐ 超大文档　☐ 混栈内容　☐ 未命中（跳过） |
| 非 Java 服务端部分 | 已忽略（见 §1） |
| Java 模块数 | <N> |
| 人确认状态 | ☐ 模块边界　☐ 模块需求 md　☐ 开发顺序 |
| 本批次时间戳目录 | `docs/req-intake-<YYYYMMDDHHMMSS>/` |

## 1. 非 Java 服务端处理

> 已忽略非 Java 服务端部分（不列入清单、不产文件）。

## 2. Java 模块构成清单

| 模块 | 切分依据（业务域/章节/依赖） | 覆盖原文章节 | 与其他模块依赖 | 建议开发顺序 |
|---|---|---|---|---|
| <模块名> | <依据> | §… | 依赖 <模块> | 1 |
| … | | | | |

## 3. 跨模块依赖与开发顺序

- 依赖关系：<A→B：A 依赖 B 的 XXX 接口 / 数据>
- 推荐顺序：<模块名> → <模块名> …

## 4. 人确认记录

| 确认项 | 结果 | 备注 |
|---|---|---|
| 模块边界 / 范围 | ☐ 通过 / 修改 | |
| 模块需求 md 来源完整性 | ☐ 通过 / 修改 | |
| 开发顺序 | ☐ 通过 / 修改 | |
| 其他（口头补充需求等） | | |

> 未获全部确认前，任何模块不得进入 1.1 `/feature-list`。
