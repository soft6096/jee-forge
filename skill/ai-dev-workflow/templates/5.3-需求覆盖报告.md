# 需求覆盖报告

> 使用说明：这是第 ⑤ 步"需求覆盖"（**5.3**）的产物模板。**由独立步骤生成**（非编码 Agent 自评）——逐需求点回读核对代码，证明需求"没漏做"，输出本报告。**产物落盘命名 = `5.3.<功能项序号>-<功能名>-需求覆盖报告.md`**（功能项序号与 3.x 技术方案一致，如功能项 6 → `5.3.6-前台商品列表与详情-需求覆盖报告.md`；禁止拼任务 ID）。
> 前置：**5.2.<功能项序号>-<功能名>-规范核对报告.md**（5.2 节点 `/check-standards` 独立核对产出，编码 Agent 不自评；**本步骤执行前先 `ls` 确认该报告已存在且用户已确认，不存在 → 拒绝出报告，返回 `/check-standards`**）。
> 定位：**防漏做**闸门，**不是"验证通过"**。可机器验证项附 `file:line` 证据；**语义/行为项只标注"静态未验证，需按需行为验证"，禁止打 ✅**。需要运行时行为验证 → 按需用 `test-workflow` 技能（`/gen-test`）真实调用接口，不阻塞本报告。
> 双向核对：① 需求 → 代码（有没有漏做）；② 代码 → 需求（有没有需求没要求却实现的）。
> 下方示例为**订单创建（Controller）**，结论列用 `✅ 已实现` / `❌ 未找到` / `静态未验证` 填写。
>
> 📌 **样式要求**：速览框 / 对齐表格统一按 `通用-中间产物-MD样式规范.md`。

> [!IMPORTANT] 文档速览
>
> | 项目 | 内容 |
> | :--- | :--- |
> | **功能** | 订单创建（US-001，Controller） |
> | **需求点覆盖** | 8 个需求点 · 7 已实现 / 1 未找到 |
> | **反向核对** | 2 处"代码有、需求没要求"待确认 |
> | **语义/行为项** | 3 项标"静态未验证"（未打 ✅） |
> | **覆盖边界** | Listener/Job：不适用（本功能为 Controller）；无入口：无 |
> | **结论** | 有缺口（1 项）→ 追加任务补齐 / 人工确认豁免 |

> [!WARNING] 本报告不是"验证通过"
> 需求覆盖报告是**静态结构核对**：证明每个需求点都有落点或明确缺口。它**不证明代码可运行**（配置错误/连接失败/打包陈旧/建表缺失都查不出）。
> **语义/行为**类结论一律标"静态未验证，需按需行为验证"，**禁止打 ✅**；需要行为验证时按需走 `test-workflow`（`/gen-test`）真实调用接口，不阻塞主流程。

---

## 报告信息

| 项目 | 内容 |
| :--- | :--- |
| 功能 | 订单创建（US-001） |
| 1.1 功能清单 | docs/订单模块V1.2.0-20260824103000/1.1-功能清单.md |
| 3.x.1 技术方案 | docs/订单模块V1.2.0-20260824103000/3.1.1-订单创建-技术方案.md |
| 3.x.2 接口清单 | docs/订单模块V1.2.0-20260824103000/3.1.2-订单创建-接口清单（前后端通用）.md |
| 5.2 规范核对报告 | docs/订单模块V1.2.0-20260824103000/5.2.1-订单创建-规范核对报告.md（已存在 ✅） |
| 核对日期 | 2026-08-07 |
| 核对人（Agent/人） | Agent（独立步骤产出，人复核） |

---

## 1. 逐需求点核对（需求 → 代码，防漏做）

> 每行 = 1.1 功能清单里的**一个需求点 / 验收标准**；证据必须落到 `代码位置(file:line)`，找不到 → 标"未找到（缺口）"；语义/行为类需求点结论列写"静态未验证"。

| # | 需求点（来自 1.1） | 对应代码 file:line | 证据摘要 | 结论 |
| :---: | :--- | :--- | :--- | :---: |
| 1 | 创建订单：接收商品+数量，生成订单号 | `OrderController.java:42`、`OrderServiceImpl.java:88` | 端点 POST /order；orderNo 生成逻辑 | ✅ 已实现 |
| 2 | 金额校验：金额必须 > 0 | `OrderCreateDTO.java:25` | `@DecimalMin` 校验注解 | ✅ 已实现 |
| 3 | 商品不存在时返回 400 | — | 未找到对应错误码映射 | ❌ 未找到（缺口） |
| 4 | 金额计算含折扣/运费 | `OrderServiceImpl.java:120` | 存在计算逻辑，取值/边界正确性无法静态判定 | 静态未验证 |

## 2. 反向核对（代码 → 需求，防多做/偏做）

> 遍历本功能项目标范围内代码，找出"**需求没要求、代码里却有**"的东西，逐条待确认。

| # | 代码里的内容 file:line | 现象 | 需求是否要求 | 处理 |
| :---: | :--- | :--- | :---: | :--- |
| 1 | `OrderController.java:71` | 额外提供 GET /order/list 列表接口 | 否 | 待确认（1.1 无此功能项 → 移出 / 补需求） |
| 2 | `OrderCreateDTO.java:31` | 自增 remark 备注字段 | 否 | 待确认（是否保留） |

---

## 3. 两类结论分列（严禁混淆）

### 3.1 可机器验证（附证据）

> 端点 / 字段 / 校验规则 / 错误码 / DDL 是否存在——附 `file:line`，判 `✅ 已实现` / `❌ 未找到`。

| 类别 | 核对项 | 证据 file:line | 结论 |
| :--- | :--- | :--- | :---: |
| 端点 | POST /order 存在 | `OrderController.java:42` | ✅ |
| 字段 | orderNo / amount 已定义 | `OrderCreateDTO.java:18` | ✅ |
| 校验 | 金额 > 0 | `OrderCreateDTO.java:25` | ✅ |
| 错误码 | 商品不存在 → 400 | — | ❌ 未找到 |
| DDL | sys_order 表结构 | `db/schema.sql:12` | ✅ |

### 3.2 语义 / 行为（静态未验证，禁止打 ✅）

> 业务规则是否正确、状态流转 / 事务 / 并发行为是否符合预期——静态核对无法证明运行时行为，**一律标注，不得打 ✅**。

| # | 语义 / 行为项 | 静态核对说明 | 结论 |
| :---: | :--- | :--- | :--- |
| 1 | 金额计算含折扣/运费是否正确 | 代码存在计算逻辑，数值正确性无法静态判定 | 静态未验证，需按需行为验证 |
| 2 | 库存扣减并发下不超卖 | 需运行时并发场景 | 静态未验证，需按需行为验证 |
| 3 | 订单状态流转是否符合状态机 | 需运行时按序操作验证 | 静态未验证，需按需行为验证 |

---

## 4. 覆盖边界（如实标注）

| 入口 | 本报告处理 | 说明 |
| :--- | :--- | :--- |
| Controller（HTTP） | 逐需求点核对 | 见上 |
| Listener（MQ） | **未验收** | 缺投递/触发手段；**不阻塞收尾** |
| Job（定时） | **未验收** | 缺手动触发手段；**不阻塞收尾** |
| 无入口（框架回调 / 启动期 / 纯工具） | **未覆盖** | 无入口形态，如实注明 |

---

## 5. 结论

- [ ] 全部需求点已实现（逐需求点无 ❌ 未找到）
- [ ] 有缺口（见第 1 节，需追加任务补齐 / 人工确认豁免）
- [ ] 反向核对无待确认项
- [ ] 语义/行为项已统一标注"静态未验证，需按需行为验证"（**未打 ✅**）
- [ ] 缺口已处理：追加任务回 `/implement` 补齐 / 人工确认豁免（理由：____）
- [ ] Listener/Job 未验收项已标注（不阻塞收尾）

> [!WARNING] 结论判定
> 有任意需求点"未找到（缺口）"且未处理 → 结论为"有缺口"，追加任务回 `/implement` 补齐或人工确认豁免后方可关闭。
> **语义/行为项一律不得标为"通过"**——需要运行时验证时按需走 `test-workflow`（`/gen-test`）。本报告只证明"没漏做"，不证明"能跑通"。
