# 澄清问题清单 - <模块名>

> 使用说明：这是 Clarify 环节的产物模板。**先扫 `docs/追问模式库-Java.md`（机械信号命中必问，同一字段只出优先级最高一条）→ 再逐功能项盘问歧义/入口复杂度/边界/依赖**，两类问题合并成同一份清单填写。
> 每个问题由 AI 给出**建议默认值**，人确认后状态改为"已确认"；全部确认后才能进入技术方案。1.2（req-gate）已裁决/已标 🔲 的项直接承接，不重复问。
> 示例为**订单模块**，复制后替换成你的业务。
>
> 📌 **样式要求**：速览框 / 对齐表格统一按 `通用-中间产物-MD样式规范.md`。

> [!IMPORTANT] 文档速览
>
> | 项目 | 内容 |
> | :--- | :--- |
> | **澄清对象** | 订单管理功能清单 |
> | **问题总数** | 8（已确认 5 · 待确认 3） |
> | **阻塞项** | 2（见下方 🔲 项） |

## 澄清信息

| 项目 | 内容 |
| :--- | :--- |
| 功能清单 | docs/订单模块V1.2.0-20260824103000/1.1-功能清单.md |
| 澄清日期 | 2026-08-07 |
| 澄清人（AI/人） | AI 盘问，人确认 |

## 问题清单

| # | 涉及功能项 | 类型/来源 | 问题 | AI 建议默认值 | 状态 |
| :---: | :---: | :---: | :--- | :--- | :---: |
| 1 | #4 订单取消 | 歧义 | 已发货的订单能否取消？ | 不允许，返回 409 + 提示"已发货不可取消" | ✅ |
| 2 | #4 订单取消 | 边界 | 重复取消请求如何处理？ | 幂等：已取消则直接返回成功 | ✅ |
| 3 | #4 订单取消 | 边界 | 部分发货状态能否取消？ | 不允许，返回 409 | ✅ |
| 4 | #1 订单创建 | 依赖 | 是否依赖库存服务？ | 是，调用 StockService 扣减库存 | ✅ |
| 5 | #1 订单创建 | 边界 | 库存扣减失败的处理策略？ | 事务回滚，返回 400 + "库存不足" | ✅ |
| 6 | #1 订单创建 | 模式库 P1（计算vs录入） | "amount"是用户录入还是系统按单价×数量-优惠计算？含运费/优惠吗？ | 系统计算，含运费减优惠，公式写入技术方案 | 🔲 |
| 7 | #3 订单支付回调 | 模式库 P9（状态集） | 重复支付消息（同订单两次 PAID）？PAID 是否有入口唯一约束 | 幂等：唯一键 order_id+status 跳过 | 🔲 |
| 8 | #6 订单超时关闭 | 模式库 P9（状态集） | CLOSED 由"超时关闭"置入，枚举里是否有该迁移边？与手动取消并发？ | 状态位防重入，先到先得 | 🔲 |

> [!WARNING] 阻塞项
> 以下问题必须人确认后才能进入技术方案：#6 金额口径、#8 超时与手动取消并发。

## 决策记录（可选）

| # | 决策 | 备注 |
| :---: | :--- | :--- |
| #6 | 采用 AI 默认值（金额=含运费减优惠） | 已记录到技术方案 |
| #1 | 依赖库存服务为人工确认补充 | — |

## 完成标准自检

- [ ] **追问模式库已扫描**（docs/追问模式库-Java.md）：命中信号已全部进问题清单（未自己选答案）；同一字段重复命中已按优先级去重；无命中时注明"模式库无命中"
- [ ] 每个功能项无未确认的歧义
- [ ] 每个功能项的边界用例已列出（合法/非法/边界）
- [ ] 依赖完整标注（功能间 + 外部系统）
- [ ] 1.2 交来的矛盾/术语 🔲 项已全部裁决（未重复追问）
- [ ] 问题清单状态列无"待确认"残留
- [ ] 功能清单已更新（边界/风险列 + 依赖完善）
- [ ] 文档开头有速览框；表格分隔行带对齐标记
