---
description: 脚手架检查清单（从零生成工程必做，第⓪步）。触发词：项目初始化/脚手架/生成项目/从零创建/新建工程。用法：/0.0-项目初始化 <项目路径>。核心：生成 Spring Boot 工程后逐项核对 10 项基础设施，缺一项即不合格。
---

# 项目/工程初始化（脚手架检查）

从零生成/初始化 Spring Boot 工程（含 AI 一次生成整个项目）时必做。**既有项目迭代跳过本节**（老代码 → 0.5 存量扫描；已接入规范项目 → 1.1）。

## 输入

- 项目路径（从零生成的 Spring Boot 工程）

## 执行

1. 生成工程后，**逐项核对脚手架硬性检查清单**（参考模板 `templates/0.0-项目初始化.md`），**缺一项即不合格**：

- [ ] 配置文件四件套：`application.yml` + `-dev` + `-qa` + `-online`，profile 激活 `${SPRING_PROFILES_ACTIVE:dev}` 不写死
- [ ] HikariCP 显式池参数（maximum-pool-size / minimum-idle / pool-name 等），禁止 datasource 三件套裸奔
- [ ] JDBC URL 字符集：**不写 `characterEncoding=utf8mb4`**，不写或 `characterEncoding=UTF-8`；`serverTimezone=Asia/Shanghai` 必带
- [ ] JDBC URL 端口与环境实际一致（禁止默认 3306 想当然）
- [ ] MyBatis-Plus 插件配置（分页 + 乐观锁，参考 java-code-standards `04-templates/ConfigTemplate.java`）
- [ ] MetaObjectHandler 实现（createTime/updateTime 自动填充，参考 `04-templates/MyMetaObjectHandler.java`）
- [ ] 建库/建表显式 utf8mb4；库初始化按当前 schema.sql 全量重建，禁止沿用旧库旧表
- [ ] MySQL 驱动 Connector/J 8.x（Spring Boot BOM 管理，无 5.1 旧驱动）
- [ ] 日志配置：`resources/logback-spring.xml` 已提供（控制台 + 滚动文件 + 环境级 level，参考 java-code-standards `04-templates/logback-spring.xml`）；application.yml 有 logging.level 覆盖
- [ ] 接口文档依赖：接口模块已引 springdoc-openapi（/knife4j）且版本由 BOM 管理；`springdoc.api-docs.enabled` 按环境配置（online 关闭）

2. **启动验证**：`mvn spring-boot:run`（或项目对应启动命令）——启动无报错 + 含插入接口调通 + 各 profile 可切换

## 输出

核对结果（10 项逐项 ✅/❌）+ 启动验证证据，落盘 `docs/0.0-脚手架核对.md`

## 完成标准

- [ ] 10 项全部通过（缺一项即不合格）
- [ ] 启动验证通过（启动日志 + 插入接口调通 + profile 切换证据）


---

## 进度记录（通用-模块进度与断点.md）

本步骤产物经人确认后，同步更新模块版本目录 `docs/<模块名>V<版本>-<时间戳>/通用-模块进度与断点.md`：

- 对应步骤行 → `✅ 已确认`；「当前断点」→ 下一步骤；「变更记录」追加一行（日期/更新人/说明）
- 模块目录尚未创建（0.0 / 0.5 / 0.9 req-intake 等前置阶段）→ 跳过；1.1 建目录时创建 `通用-模块进度与断点.md` 并据已有产物回填
