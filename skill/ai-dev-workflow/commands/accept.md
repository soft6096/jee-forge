---
description: 需求覆盖报告（第⑤步，独立生成、不由编码 Agent 自评）。触发词：需求覆盖/覆盖报告/防漏做/accept/需求对代码。用法：/accept <功能名或技术方案路径>。核心：逐需求点回读核对代码，每个需求点给 `file:line` 证据或标"未找到（缺口）"；双向核对（需求→代码 + 代码→需求）；两类结论分列（可机器验证 / 语义待行为验证，**语义项禁止打 ✅**）。定位是"防漏做"闸门，不是"验证通过"；行为验证按需走 test-workflow（/gen-test），不阻塞主流程。
---

# 需求覆盖报告（防漏做核对）

当前功能项代码实现完成后，**由独立步骤**（非编码 Agent 自评）逐需求点回读核对代码，输出**需求覆盖报告**——证明"需求点的每一项都有落点或明确缺口"。

> 定位：这是**防漏做**闸门，**不是"验证通过"**。可机器判定的结构项附证据；语义/行为项只标注"静态未验证，需按需行为验证"，**禁止打 ✅**。需要运行时行为验证（接口真的能调通/事务并发行为正确）时，**按需**用 `test-workflow` 技能（`/gen-test`）真实调用接口，不阻塞本报告。

## 输入

- `1.1-功能清单.md`（本功能项 + 承接需求点 + 验收标准 GIVEN-WHEN-THEN）
- `3.<功能项序号>.1-<功能名>-技术方案.md`（验收场景三态：合法 / 非法 / 边界）
- `3.<功能项序号>.2-<功能名>-接口清单（前后端通用）.md`（Controller 类型；URL / 方法 / 入参 / 出参）
- 5.1 已实现代码（`src/main/java/...`）
- **`docs/<模块名>V<版本号>-<YYYYMMDDHHMMSS>/5.2.<功能项序号>-<功能名>-规范核对报告.md`（前置硬依赖，先检查存在性）**

## 执行

0. **前置检查（硬性闸门）：5.2 规范核对报告必须已存在**——`ls docs/*/5.2.*-*.md` 确认 `5.2.<功能项序号>-<功能名>-规范核对报告.md` 已由 `/check-standards`（5.2 节点）产出且用户已确认通过：
   - **报告不存在 → 拒绝生成需求覆盖报告，提示先执行 `/check-standards` 完成规范核对**（禁止跳过核对直接出报告、禁止用"编码 Agent 自查结果"替代）
   - 报告存在但含未解决 ❌ → 先追加任务返回 /implement 补齐 → 重新 /check-standards → 再回来

1. **逐需求点核对（需求 → 代码，防漏做）**：读 1.1 功能清单与本功能项验收标准，逐条回读代码定位落点：
   - 每个需求点 / 验收点 → 找对应实现 → 记 **`代码位置(file:line)`** 或 **`未找到（缺口）`**
   - **证据必须落到 `file:line`**（类 / 方法 / 校验 / 错误码 / DDL），禁止凭方案或凭记忆写"应该实现了"

2. **反向核对（代码 → 需求，防多做/偏做）**：遍历本功能项目标范围内的代码，找出"**需求没要求、代码里却有**"的东西（自创字段 / 额外端点 / 超出范围的逻辑）→ 记入"反向核对"表，标"需求未要求"待确认。

3. **两类结论分列（严禁混淆）**：
   - **① 可机器验证**（端点 / 字段 / 校验规则 / 错误码 / DDL 是否存在）：附证据 `file:line`，判 `✅ 已实现` / `❌ 未找到`。
   - **② 语义 / 行为**（业务规则是否正确、状态流转 / 事务 / 并发行为是否符合预期）：**显式标注"静态未验证，需按需行为验证"**，**禁止打 ✅**（静态核对无法证明运行时行为）。

4. **覆盖边界如实标注**：

   | 入口 | 本报告处理 |
   |---|---|
   | Controller（HTTP） | 逐需求点核对（可机器验证项附证据 + 语义项标未验证） |
   | Listener（MQ） | 默认标"**未验收**"（缺投递/触发手段），**不阻塞收尾** |
   | Job（定时） | 默认标"**未验收**"（缺手动触发手段），**不阻塞收尾** |
   | 无入口（框架回调 / 启动期 / 纯工具） | 标"**未覆盖**"，如实注明 |

5. **缺口闭环**：未找到项 → 产出**缺口清单**（缺口 + 对应需求点 + 处理建议）→ 向用户展示确认 → 追加任务回 `/implement` 补齐，或**人工确认豁免**（写明理由，禁止拿豁免当借口）；Listener/Job"未验收"项**不阻塞**收尾。处理完成后回本步更新报告。

## 输出

`docs/<模块名>V<版本号>-<YYYYMMDDHHMMSS>/5.3.<功能项序号>-<功能名>-需求覆盖报告.md`（模板 `templates/5.3-需求覆盖报告.md`；功能项序号与 3.x 技术方案一致）

## 完成标准

- [ ] **前置检查通过：`5.2.<功能项序号>-<功能名>-规范核对报告.md` 已存在**（/check-standards 产出 + 用户确认）——报告不存在则拒绝出报告，回 /check-standards
- [ ] 逐需求点核对表完整：每需求点有 `file:line` 证据或明确"未找到（缺口）"，无凭记忆结论
- [ ] 反向核对完成：需求没要求、代码里却有的内容已列出并标"需求未要求"
- [ ] 两类结论分列：可机器验证项附证据；**语义/行为项标"静态未验证，需按需行为验证"且未打 ✅**
- [ ] 覆盖边界如实：Listener/Job 标"未验收"（不阻塞）、无入口标"未覆盖"
- [ ] 缺口已产出清单并经人确认处理（补齐 / 豁免 + 理由），无静默缺口
- [ ] 报告经人确认后落盘（`5.3.<功能项序号>-<功能名>-需求覆盖报告.md`）


---

## 进度记录（通用-模块进度与断点.md）

本步骤产物经人确认后，同步更新模块版本目录 `docs/<模块名>V<版本>-<时间戳>/通用-模块进度与断点.md`：

- 对应步骤行 → `✅ 已确认`；「当前断点」→ 下一步骤；「变更记录」追加一行（日期/更新人/说明）
- 模块目录尚未创建（0.0 / 0.5 / 0.9 req-intake 等前置阶段）→ 跳过；1.1 建目录时创建 `通用-模块进度与断点.md` 并据已有产物回填


---

## 经验沉淀（docs/experience.md）

需求覆盖核对完成后，把本次开发踩坑追加一条到项目 `docs/experience.md`（模板 `templates/通用-经验记录.md`；跨项目通用标 [HARD] 待上缴，否则标本地）；`✅ 已固化` 历史条目移入归档。不存在该文件 → 用模板创建后追加。
